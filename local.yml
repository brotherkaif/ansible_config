---
- hosts: localhost
  connection: local
  become: true

  tasks:
    - name: install base packages
      package:
        name:
          - tmux
          - tree
          - htop
          - git
          - pass
          - ffmpeg

    - name: install stupid terminal nonsense
      package:
        name:
          - figlet
          - toilet

    - name: DOTFILES bash
      copy:
        src: 'files/dotfiles/bash/.bashrc'
        dest: '~/.bashrc'
        owner: kaifahmed
        group: kaifahmed
